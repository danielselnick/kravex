[project]
name = "kravex-demo"
version = "0.2.0"
description = "In a world where search indices must migrate... one script dared to orchestrate."
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31",
]
